# komodo-mon
A unique tool that takes known information about a person and generates similar usernames in an attempt to discover other aliases. It also supports passive scanning and records the times that usernames are created or deleted.
